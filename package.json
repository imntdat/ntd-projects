{
  "name": "@ntd/root",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build:components": "yarn workspace @ntd/components build",
    "start:core": "yarn workspace @ntd/core dev"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
